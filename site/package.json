{
  "name": "@openresult/site",
  "version": "0.1.1",
  "description": "openresult.dev — the specification, the examples, and a viewer anything can post to.",
  "license": "Apache-2.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsx generate/render-markdown.ts && vite build",
    "preview": "vite preview",
    "serve": "node server/index.mjs"
  },
  "dependencies": {
    "@openresult/core": "workspace:*",
    "@openresult/validate": "workspace:*",
    "@openresult/viewer": "workspace:*"
  },
  "devDependencies": {
    "marked": "^14.1.4"
  }
}
